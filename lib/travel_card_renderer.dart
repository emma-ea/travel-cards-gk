import 'package:flutter/material.dart';

class TravelCardRenderer extends StatelessWidget {
  const TravelCardRenderer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}